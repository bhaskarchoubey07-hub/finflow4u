export const metadata = {
  title: "Borrower Dashboard | LendGrid",
  description: "Apply for loans, view your active loans, and manage repayments with transparent pricing.",
};

export default function BorrowerLayout({ children }) {
  return children;
}
