export const metadata = {
  title: "Loan Marketplace | LendGrid",
  description: "Explore open lending opportunities. Compare borrower scores, funding progress, and expected yields.",
};

export default function MarketplaceLayout({ children }) {
  return children;
}
