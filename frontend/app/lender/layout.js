export const metadata = {
  title: "Lender Dashboard | LendGrid",
  description: "Monitor capital deployment, track portfolio yields, and manage your wallet balances.",
};

export default function LenderLayout({ children }) {
  return children;
}
